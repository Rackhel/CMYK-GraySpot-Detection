# ── Core ML & Deep Learning / 핵심 ML 및 딥러닝 ─────────────────────────
torch>=2.1.0
torchvision>=0.26.0
# torchmetrics>=0.11.0 # only needed for advanced metric tracking

# ── Data Processing & Analysis / 데이터 처리 및 분석 ───────────────────────
# numpy<2 제약: Anaconda 기본 환경의 pyarrow/pandas가 NumPy 1.x 기준 컴파일
# numpy<2 constraint: Anaconda default env's pyarrow/pandas compiled against NumPy 1.x
numpy>=1.24.0,<2.0
pandas>=2.0.0
opencv-python>=4.8.0
Pillow>=12.2.0
scikit-learn>=1.5.0
scipy>=1.11.0
albumentations>=1.3.0 # only needed for advanced data augmentation

# ── Hyperparameter Tuning / 하이퍼파라미터 튜닝 ──────────────────────────────
optuna>=3.4.0

# ── Configuration & Utilities / 설정 및 유틸리티 ─────────────────────────────
PyYAML>=6.0
python-dotenv>=1.0.0 # only needed if using .env files for configuration

# ── Visualization & Reporting / 시각화 및 리포팅 ────────────────────────────
plotly>=5.17.0
matplotlib>=3.7.0
seaborn>=0.12.0
Jinja2>=3.1.0 # only needed for HTML report generation

# ── Data Download / 데이터 다운로드 ──────────────────────────────────────────
# gdown>=4.7.0  # only needed for initial dataset download

# ── Testing / 테스트 ────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=7.1.0

# ── Dimension Reduction / 차원 축소 ─────────────────────────────────────────
umap-learn>=0.5.4

# ── GUI & Web Interface / GUI 및 웹 인터페이스 ────────────────────────────────
streamlit>=1.28.0 # only needed for web-based GUI
pyinstaller>=6.0.0 # only needed for building GUI executable

# ── Code Quality & Formatting / 코드 품질 및 포매팅 ─────────────────────────────
black>=23.3.0
isort>=5.12.0

# ── ONNX & Model Export / ONNX 및 모델 내보내기 ───────────────────────────────
onnx>=1.13.0
onnxscript>=0.0.1
